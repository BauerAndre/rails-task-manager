<div class="p-5 m-5">
  <h2><%= @task.title %></h2> <%=link_to "Edit", edit_task_path%>
  <p><%= @task.details %></p>
  <p><%= @task.completed %></p>
  <br>
    <%= form_for(@task) do |f| %>
    <%= f.label :completed %>
    <%= f.check_box :completed %>
    <%= f.submit %>
     <% end %>
  <br>
  <%= link_to "add new task", new_task_path %>
  <br>
  <%=link_to "back to the list", index_path%></li>
</div>
